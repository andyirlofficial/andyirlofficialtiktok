# TikTok Follower JSON API

A Flask app that returns the TikTok follower count for user `andyirlofficial` at `/followers.json`, updated every 5 seconds.

---

## 🔧 Setup

### 1. Install dependencies
```bash
pip install -r requirements.txt
